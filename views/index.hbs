<h1>Slapjack</h1>

<button type="button" id="startGame" onclick="start()" class="btn btn-primary">Start Game</button>
<button type="button" onclick="playCard()" class="btn btn-primary">Play Card</button>
<button type="button" onclick="slap()" class="btn btn-success">Slap!</button>

<img id="card" src="" style="width: 200px; margin-top: 50px">
<div id="messages-container"></div>

<script src="/socket.io/socket.io.js"></script>
<script>
  var socket = io();
  var username = "";
  var id = "";

  socket.on('connect', function() {
    console.log('connected');
  });

  socket.on('username', function(resp) {
    // console.log("this is resp",resp)
    // console.log("this is equals", resp===false)
    if(resp == false){
      var answer = prompt("enter username");
      console.log(resp)
      socket.emit("username", answer)
    }else{
      id = resp;
    }
  });

  socket.on('playCard', function(data) {
  var str = data.card.cardString.toLowerCase().split(" ").join("_") + ".svg";
    $("#card").attr("src", str);
  });

  socket.on('slap', function(data) {
    handleMessage(data.slap.message);
  });

  socket.on('start', function(data) {
    //startGame();

    
  });

  socket.on('message', function(data) {

  });

  socket.on('disconnect', function() {
    // refresh on disconnect
    window.location = window.location;
  });

  function start() {
    //socket.emit('start');
    //startGame();
    $('#startGame').on('click',function(){
      $(this).prop('disabled',true);
    });
  }

  // function startGame(){
  //   $('#startGame').on('click',function(){
  //     $(this).prop('disabled',true);
  //   })
  // }

  function playCard() {
    socket.emit('playCard');
  }

  function slap() {
    socket.emit('slap');
  }

</script>
